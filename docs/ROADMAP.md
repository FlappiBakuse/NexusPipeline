# NexusPipeline 后续开发路线（Roadmap）

**编制日期**：2026-08-21｜ **当前基线**：v0.8.4（Pre-release）｜ **发布模式**：v1.0.0 前一律 Pre-release、直接 push main；v1.0.0 起仅 PR 合入

> 本文档为**当前已发布版本之后的开发路线**：已交付版本的详细内容见 [CHANGELOG.md](../CHANGELOG.md)，不在此重复。每个版本列出任务项、关键设计点、难度与验证要求。开工前先读项目 `AGENTS.md`（构建/测试顺序、运行时数据、环境陷阱、前端强约束）与本文件对应版本章节。**版本开工前**：打本地备份 tag `backup/vX.Y.Z-dev`（不 push）+ 同步 `src/NexusPipeline.csproj` `<Version>`。
> 已知问题台账（仅未修复项）见 [KNOWN-ISSUES.md](KNOWN-ISSUES.md)；历史全面评估报告见 [archive/ASSESSMENT-v0.6-v0.7.md](archive/ASSESSMENT-v0.6-v0.7.md)。

---

## 版本路线总览

| 版本 | 主题 | 难度 | 状态 |
|---|---|---|---|
| v0.6.10 | **长时脚本（-1 无限超时）** + 队列弹窗拖拽/任务卡片化 + 文档体系重组 | ⭐⭐⭐ | ✅ 已发布（2026-08-15） |
| v0.7.0 | **安卓模拟器（adb 方案）** | ⭐⭐⭐ | ✅ 已发布（2026-08-15） |
| v0.7.1 | 已知问题快赢修复（KN-02/51/52/53/54/56）+ 关键字跨日志 AND + 前端优化 | ⭐⭐ | ✅ 已发布（2026-08-15） |
| v0.7.2 | 已知问题 A 类修复（KN-01/03/04/10/32/36/42：损坏配置保留/队列防重入/并发加锁/密钥回显/门禁泄漏等） | ⭐⭐⭐ | ✅ 已发布（2026-08-16） |
| v0.7.3 | **前端设计全面重构（Web Interface Guidelines）**：limits/模态焦点链补齐、dispatch 局部更新、拖拽键盘替代、内联错误、提交忙碌态、ARIA 全量补全、设置自动保存/重启按钮移位/分页按需显示（KN-12/13/14/15/16/58-64） | ⭐⭐⭐ | ✅ 已发布（2026-08-16） |
| v0.7.4 | **已知问题批量修复**：CLI 删除清理（KN-05）、判定竞态（KN-11）、大小写一致（KN-37）、Webhook 转义/白名单单源（KN-34/26）、settings 单次保存（KN-30）、插件开关校验（KN-40）、resolve 多占位符校验（KN-07）、死代码全量清理（KN-17~29/38/41/43/44/45）、基础按钮 40px（KN-14）、侧栏地址动态化（KN-47）、台账外 KN-65/66/67（目录型模板误删/系统操作叠加/归档兜底断言） | ⭐⭐⭐ | ✅ 已发布（2026-08-16） |
| v0.7.5 | 已知问题收尾：远程图标 fetch blob（KN-06）、历史详情窗口（KN-35）、AuthFails 清理（KN-31）、Python 候选排序（KN-39）、Webhook 状态码（KN-33）、bat 等待缩放（KN-08）、.session/.meta PascalCase（KN-55）、icon 安全头（KN-27）；台账外 KN-68~72（CSP 跨端口轮询/maaend 实例回退/编辑 start 孤儿窗口/退出轮双触发/KillByName 连带杀游戏） | ⭐⭐⭐ | ✅ 已发布（2026-08-16） |
| v0.7.6 | **自动更新配置（config → store 反向同步）**：运行产生的配置更改回写用户快照（任务完成记录/运行计数/脚本更新新增任务保留延续），开关默认开 + 15 秒首次检测；专项插队文件按还原描述（config-restore.json）还原启停后入快照；maaend/bettergi 判断脚本重写 | ⭐⭐⭐ | ✅ 已发布（2026-08-16） |
| v0.7.7 | KN-77 自动更新配置内容守护修复（收尾同步内容有效性探测 + 稳定性双采样扩展）+ 文档全面对齐 v0.7.6 | ⭐ | ✅ 已发布（2026-08-16） |
| v0.7.8 | 服务复用、运行生命周期收敛、完整重试配置交换、自动更新配置事务化、队列全局串行 | ⭐⭐⭐⭐ | ✅ 已发布（2026-08-17） |
| v0.7.9 | **核心模块扩展性治理**：RunBudget/attempt 收尾、ConfigRunSession、Plugin capability registry、显式 PluginHostServices、行为兼容测试与文档治理 | ⭐⭐⭐ | ✅ 已发布（2026-08-19） |
| v0.7.10 | 前端交互稳定性与已知问题收尾：分页拖拽、远程重启、历史完整日志、令牌交互、调度确认、KN-74/75/76 | ⭐⭐⭐ | ✅ 已发布（2026-08-19） |
| v0.7.11 | **前端 UI 全面精修**：SVG 图标、间距/按钮组/表单栅格、Modal 分层滚动、拖拽滚动恢复、移动端布局与错误反馈 | ⭐⭐⭐ | ✅ 已发布（2026-08-20） |
| v0.7.12 | **前端 UI 第二次全面优化**：第二轮 Design System 收敛、Dashboard 新布局、移动端重排、危险操作语义与 E2E flake 修复 | ⭐⭐⭐ | ✅ 已发布（2026-08-20） |
| v0.8.0 | **后端架构强化**：应用启动边界、配置交换恢复拆分、运行状态存储隔离、兼容性回归测试 | ⭐⭐⭐⭐ | ✅ 已发布（2026-08-20） |
| v0.8.1 | **后端领域边界收敛**：运行编排拆分、配置事务边界、插件 capability 消费隔离、Web/CLI 命令入口收敛、架构治理测试 | ⭐⭐⭐⭐ | ✅ 已发布（2026-08-20） |
| v0.8.2 | **后端架构第三次优化**：DispatchCenter 职责拆分、显式业务边界、RuntimeContext 依赖收敛、架构保护测试 | ⭐⭐⭐⭐ | ✅ 已发布（2026-08-21） |
| v0.8.3 | **项目与文档治理**：单一事实源、社区安全文件、测试目录拓扑与工程配置规范化 | ⭐⭐⭐ | ✅ 已发布（2026-08-21） |
| v0.8.4 | **前端视觉重构**：低阴影、列表优先、按钮语义、队列弹窗层级和 Dashboard 信息收敛，保留粒子效果 | ⭐⭐ | ✅ 已发布（2026-08-22） |
| 后续功能 | 并行调度队列（资格矩阵收敛） | ⭐⭐⭐⭐ | 未开始（模拟器稳定后立项） |
| 后续功能 | 自动签到通用插件 | ⭐⭐⭐ | 未开始（需 API 调研） |
| 后续功能 | 桌面分身 | ⭐⭐⭐⭐⭐ | 暂缓，拆分独立技术验证 |

---

## v0.8.0：后端架构强化（已发布）

- [x] 创建本地 `backup/v0.8.0-dev` 基线并同步项目版本号为 `0.8.0`
- [x] 将进程入口、运行时初始化和服务启动生命周期拆分到 `Application/`
- [x] 将运行状态/已结束记录/系统操作倒计时隔离到 `ExecutionStateStore`
- [x] 将配置会话模型与配置恢复扫描/延迟重试拆分到 `Services/ConfigSwap/`
- [x] 保留现有 API、磁盘布局、配置交换协议、队列串行和取消语义
- [x] 完成管理员构建、单元测试、judge、chaos 与全量 E2E 回归
- [x] 发布前执行真实计时档全量回归，完成版本提交与 Pre-release 发布

---

## v0.8.1：后端领域边界收敛（已发布）

- [x] 创建本地 `backup/v0.8.1-dev` 基线并同步项目版本号为 `0.8.1`
- [x] 将 `RunSession` 降级为运行状态/生命周期数据对象，新增 `ExecutionCoordinator` 作为运行编排入口
- [x] 抽离单次尝试执行、重试策略、资源清理和结果汇总职责，保持现有判定/重试/收尾顺序
- [x] 建立 `ConfigurationTransaction` 配置事务边界，与现有 `ConfigRunSession`/`ConfigSwap` 兼容
- [x] 通过 capability 消费接口隔离执行域与具体插件实现，收敛通知分发职责
- [x] 增加 Application Command 入口，使 Web/CLI 复用同一运行/取消业务入口
- [x] 增加命名空间依赖与核心服务复杂度治理测试，保持现有 API、磁盘格式和队列串行语义
- [x] 完成管理员构建、单元测试、加速档 E2E、judge 与 chaos 回归
- [x] 完成发布前管理员真实计时档全量回归，并发布 v0.8.1 Pre-release

---

## v0.8.2：后端架构第三次优化（已发布）

- [x] 创建本地 `backup/v0.8.2-dev` 基线并同步项目版本号为 `0.8.2`
- [x] 建立脚本、队列、设置、执行与通知的显式应用边界，减少业务代码直接依赖 `RuntimeContext`
- [x] 拆分 `DispatchCenter` 的执行校验、运行生命周期、系统操作与审计职责，保留现有兼容入口
- [x] 让执行核心可通过 `ExecutionRequest`/`ExecutionResult` 独立验证，保持配置交换、重试、取消和队列串行语义
- [x] 增加并收紧架构边界保护测试，避免新的服务定位器和具体插件实现耦合回流
- [x] 完成管理员构建、E2E、judge、chaos 与单元测试回归，并发布 v0.8.2 Pre-release

---

## v0.8.3：项目与文档治理（已发布）

- [x] 创建本地 `backup/v0.8.3-dev` 基线并同步项目版本号为 `0.8.3`
- [x] 统一框架依赖单文件发布、队列全局串行和测试数字单一事实源规则
- [x] 将历史 `ASSESSMENT` 归档，并收敛 ROADMAP、KNOWN-ISSUES、CONTRIBUTING 与 AGENTS 的职责边界
- [x] 将测试目录整理为 `tests/NexusPipeline.Tests/` 与 `tests/e2e/`
- [x] 增加 SECURITY、Issue/PR 模板、Dependabot、EditorConfig、gitattributes 与 CI 最小权限
- [x] 完成构建、单元测试、E2E、专项稳定性测试和文档交叉一致性复核
- [x] 完成发布前真实计时档全量回归，并发布 v0.8.3 Pre-release

---

## v0.8.4：前端视觉重构（已发布）

- [x] 创建本地 `backup/v0.8.4-dev` 基线并同步项目版本号为 `0.8.4`
- [x] 按前端 Review 收敛低阴影、细边框、列表优先和信息层级，保留粒子效果
- [x] 恢复操作按钮可见描边，统一队列通知与远程访问设置行，移除队列任务列表外层卡片
- [x] 移除 Dashboard 通知推送插件中的重复状态与实例统计文案
- [x] 保留现有 API、路由、事件委托、`data-action`/`data-testid`、响应式和无障碍交互契约
- [x] 完成管理员构建、JavaScript/CSS 检查、全量 E2E、judge、chaos 与单元测试回归
- [x] 完成发布前真实计时档全量回归，并发布 v0.8.4 Pre-release（2026-08-22）

---

## v0.7.12：前端 UI 第二次全面优化与 E2E 稳定性

- [x] 收敛间距、按钮、切换控件、危险操作和页面标题的视觉语义
- [x] 按新 Review 调整 Scripts / Queues / Dashboard / Settings / Plugins / Dispatch 的布局与移动端体验
- [x] 保留现有路由、API、数据属性和核心交互契约，补充必要的可访问性状态
- [x] 合并用户提交的“新增用户后立即编辑配置”门禁释放竞态修复（`5712258`）
- [x] 完成构建、管理员 E2E 及回归验证后发布 v0.7.12 Pre-release

---

## 后续会开发的功能：并行调度队列（以当前全局串行为基线）

- [ ] **先出设计文档**：并行资格矩阵——「仅含安卓模拟器脚本的队列可并行调度，且最多再并一个非模拟器队列」；其余组合继续串行
- [ ] 放宽当前不同队列之间的全局串行限制，并建立并行资格矩阵
- [ ] 并发点改造：队列进度/历史/通知/取消/完成操作并发化；调度器触发竞态（`_runningQueueIds` 扩展为资格矩阵判定）；脚本进程冲突检测保持原子
- [ ] 测试：chaos-queue 扩展并行场景（两队列并行注入、冲突矩阵）；该功能 BUG 风险高，建议模拟器稳定后再立项

---

## 后续会开发的功能：自动签到通用插件

- [ ] **前置调研**：米游社/HoyoLab API（参考 Womsxd/MihoyoBBSTools）与森空岛 API（参考 AEtherside/skland-daily-attendance）现状与凭证格式；协议变更风险评估
- [ ] **形态**（已定）：内置 C# 通用插件（IPlugin/INotifyChannel 契约）+ **独立定时触发**（宿主内置调度，不并入脚本队列）；凭证存 SecretStore DPAPI（`enc:` 前缀）
- [ ] 前端：脚本用户编辑区新增「自动签到」按钮 + 凭证填写框（密钥语义：不回显、留空不变）
- [ ] 通知集成（签到结果走现有通知通道）；e2e 用 mock API 服务器
- [ ] 数据化插件形态不适用（需网络/定时能力），本插件走编译内置——注意与 v0.6.3「专项插件数据化」方向不冲突（签到是通用能力插件）

---

## 后续会开发的功能：桌面分身（暂缓，拆分独立评估）

- [ ] 不做入主线版本。独立研究项三块 demo：① 独立 Windows 会话创建（TS API / CreateProcessAsUser）；② RDP 画面嵌入（mstscax ActiveX 或自绘协议）；③ 非前台会话键鼠注入
- [ ] 任一 demo 不可行即降级为「窗口级方案」或集成第三方工具；评估报告先行，再决定是否立项

---

## 全局待决事项与风险

| 项 | 说明 | 归属 |
|---|---|---|
| 钉钉/飞书签名真机验证 | 已按官方规范修正，需真实机器人环境推送验证——**延后验证**（用户确认 2026-08-14） | 待真机环境 |
| 专项测试进 CI | judge 与 chaos 仅发布前本地跑；CI 超时 15 分钟撞墙风险 | 建议后续评估 |
| e2e 加速档 flake | F1-F5 已治理（v0.6.9），台账 `tests/e2e/FLAKE-LEDGER.md` 持续追踪 | v0.6.9 治理完成 |
| flake 台账 | 每次全量回归记录 flake 现象/复现条件/根因归属，直至清零 | 长期 |
| 版本号硬编码 | 01-core 已改动态读取；其余版本相关行为断言发版时核对 | 已基本解决 |
| 模拟器与并行调度的依赖关系 | 并行矩阵依赖模拟器脚本类型判定 | 后续功能规划 |
| **自动更新配置随版项** | KN-78（编码保留）、KN-79（专项恒 true 后端防御）、KN-83（还原描述稳定 ID）已纳入 v0.7.8；KN-82（首次检测阻塞主循环）仍待后续优化 | v0.7.8 |
| **判断脚本接口扩展（预留）** | 用户确认目前无计划：未来考虑在判断脚本输入中增加更多宿主接口（如窗口/进程/HTTP 探针）；记录不动作 | 长期方向 |
| **队列全局串行基线** | 当前队列内、队列间均串行；未来并行调度立项前需设计资格矩阵并补充并发测试 | 后续并行功能前置 |

---

## 开发流程提醒（AGENTS.md 摘要）

1. 版本开工：本地备份 tag `backup/vX.Y.Z-dev` + csproj 版本号同步
2. 破坏性/新增功能：备份 tag + 分步验证；改烂用 `git reset --hard backup/...` 本地还原
3. 测试分层：仅前端 → build + e2e 全量；涉及后端 → + judge + chaos + 单测；**发布前真实计时档全量**。具体数量仅记录在 CHANGELOG、Release Notes 或 CI 验证结果中
4. 提交：Conventional Commits（type 英文 + 描述中文），同版本累积一个 commit，未经要求不拆
5. 发布：`gh release create --prerelease` + zip（排除 config/）+ 成对 `.sha256`；gh 中文操作三坑（UTF-8 文件/备份原 body/输出编码）
6. 收尾：发布后更新本文件勾选状态与 KNOWN-ISSUES 台账状态
