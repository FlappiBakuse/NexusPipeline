# 项目状态（Status）

**更新日期**：2026-09-05｜**发布模式**：v1.0.0 前一律 Pre-release、直接 push main；v1.0.0 起仅 PR 合入

> 本文件记录尚未完成的开发计划、活跃技术验证和当前未解决问题。已完成版本以 [CHANGELOG.md](../CHANGELOG.md)、代码和测试结果为准。开工前先阅读项目 `AGENTS.md`，创建本地 `backup/vX.Y.Z-dev` 标签并同步版本号。

## v0.14.3 稳定性修复跟踪

- [x] 用户绑定更新保留缺失的 `configInputs`，支持显式清空和按用户替换。
- [x] `self-managed-pc-launch` 改为运行时启动计划约束，保留持久化启动参数。
- [x] 附加配置路径加入 fail-closed 准备、事务快照、启动恢复和收尾回滚。
- [x] 历史截图改用带认证的 Blob 加载，弹窗关闭时释放对象 URL。
- [x] 配置候选响应携带实际 `inputName`，前端移除输入名猜测。
- [x] PC 运行期间维护按 Attempt 隔离的最近有效截图缓存（约 1 秒一帧、最多 2 秒），游戏窗口消失时回退到缓存帧；不增加关闭前抢拍阶段。
- [x] 在真实远程环境完成手动脚本、手动队列和调度队列截图门禁；KN-90 继续验证其他窗口/进程时序。

## 后续功能：插件生态扩展

- [ ] 为更多官方 managed-code 插件补充 mock HTTP 与事件回归测试。
- [ ] 继续完善插件设置贡献的字段校验、错误呈现和目录元数据检查。
- [ ] 持续验证用户级插件数据清理、密钥保护和升级后的当前数据访问。

## 活跃技术验证与决策

- [ ] 在真实机器人环境完成钉钉/飞书签名推送验证。
- [ ] 完成更新事务的进一步故障注入矩阵，覆盖长时间运行、文件锁和异常退出组合。
- [ ] 评估历史专项测试与 Chaos 诊断工具的执行时长、隔离和维护边界。
- [ ] 持续维护运行时版本动态展示、真实计时回归和 Release 资产校验。
- [ ] 评估模拟器 capability 与并行调度矩阵的后续扩展边界。
- [ ] 评估判断脚本宿主接口扩展，如窗口、进程和 HTTP 探针能力。

## 已知问题台账

只记录当前仍未解决、能够影响当前版本或需要专项验证的缺陷与技术风险。已修复问题的版本归属见 [CHANGELOG.md](../CHANGELOG.md)；产品有意保持的行为见 [DESIGN.md](DESIGN.md) 的「已接受的设计约束」。

状态说明：

- **稳定复现**：固定输入、确定调用顺序或最小测试场景可以重复命中。
- **待专项复现**：风险方向已经确认，但需要 Windows 文件系统时序、进程生命周期或长时运行等专用环境才能进一步定量。
- **调查中**：现象或影响范围已知，复现条件尚未收敛。

| 编号 | 风险描述 | 当前代码与证据 | 影响 | 后续动作 | 状态 |
|---|---|---|---|---|---|
| KN-09 | 日志文件发生非归零截断并紧接追加时，轮询粒度内缺少可靠的截断边界；监控器可能把新旧内容的边界视为同一段，存在漏读窗口 | `src/Services/LogMonitor.cs` 在检测到长度缩短后将读取位置调整到当前文件尾部；现有回归测试覆盖截断位置策略，但尚未覆盖文件系统时序下的「截断后立即追加」窗口 | 极窄时序下可能漏掉一段用于判定的日志 | 增加 Windows 文件系统专项场景，确认可观测边界后再决定是否调整读取协议 | 待专项复现 |
| KN-73 | 配置交换共享 `Mutex` 的删除/释放与正在等待或持有锁的会话存在低概率生命周期竞态 | `src/Services/ConfigSwapPrimitives.cs` 已对等待阶段的 `ObjectDisposedException` 和释放异常做保守处理，但完整的删除并发压力证据仍不足 | 极端并发下可能需要重新获取交换锁，影响配置操作时延与可观测性 | 增加脚本删除、配置交换、恢复扫描并发的管理员专项观察，并记录是否存在现场损坏 | 待专项复现 |
| KN-90 | 专项脚本实例（如 BetterGI）任务运行成功/失败时偶发截图失败（「正在等待游戏窗口」） | 已确认的一类触发条件是脚本完成并关闭游戏早于宿主截图；当前宿主已在运行期间维护最近有效帧，窗口消失时回退有效缓存。截图链路仍依赖按进程名匹配游戏进程，其他窗口/进程切换时序尚待真实环境验证 | 极端时序下仍可能无通知截图与历史截图，任务判定不受影响 | 在真实远程手动脚本、手动队列和调度队列中验证缓存命中，并继续收集非关闭竞态的窗口/进程条件 | 待专项复现 |

## 维护规则

- 发布完成后，从「后续功能」与「活跃技术验证」移除对应完成项，在 `CHANGELOG.md` 记录发布内容。
- 未解决且影响当前版本的问题进入上方台账；修复、失效或确认为设计契约后立即移出，历史由 CHANGELOG 与 Git 保留。
- 新条目必须先确认当前代码路径、回归测试和影响范围，再写入台账。
- 发现文档与代码/测试冲突时，先判断 intended contract；涉及产品行为的决定需要单独确认，不通过文档修改掩盖冲突。
- 新版本开发必须先创建本地开发基线备份并同步版本号；版本开发期间不拆分发布。
