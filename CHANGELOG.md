# Changelog

本仓库所有重要变更均按版本记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本使用 `major.minor.patch`、`-beta.N` 或 `-rc.N` 的受限格式；GitHub Release 分类遵循宿主项目发布策略：`major=0` 或带 `-beta.N` / `-rc.N` 后缀的版本为 Pre-release，`major>=1` 且无后缀的版本为正式 Release。

## v0.16.8（Pre-release）

发布日期：2026-09-23。

### 专项任务与配置诊断

- 首发专项任务协议 `0.1.0`：任务发现、阶段报告、日志事实、选择性重试及本地化文本使用同一受校验契约；旧开发协议版本不作为正式兼容入口。
- 配置编辑和运行准入可显示插件声明的配置规则与环境检查结果，保存反馈、未知状态和错误来源分别处理；旧版未声明专项协议的插件仍可使用原有配置校验脚本。
- 任务进度、运行历史和通知展示专项任务报告，并保持旧记录可读。

### 可靠性与验证

- 加强配置交换、恢复和重试阻断时的事实保留；补充资源身份、配置隔离和更新事务的回归验证。
- 扩展隔离 Test Host、真实 Jint 专项适配器联调及架构门禁，保留正式发行程序的管理员权限要求。

## v0.16.7（Pre-release）

发布日期：2026-09-20。发行资产见 [v0.16.7](https://github.com/FlappiBakuse/NexusPipeline/releases/tag/v0.16.7)。

### 架构与插件通道

- 后端按业务模块划分唯一职责，清理全局服务定位与跨模块循环依赖，保留现役配置和事务协议。
- 官方专项插件前端由宿主实现；插件商店支持配置文件选择 stable/develop，同版本预览更新按包哈希判断，禁止自动降级。

### 验证与发布安全

- 功能测试统一使用隔离 Test Host，完整资格固定执行 H1–H5，包括更新与执行真实计时验收；正式发行仍要求管理员运行。
- 架构按 production/test-host 独立查环，测试实例显式释放；测试进程按创建身份确权，超时有界收尾。
- 新检出自动准备测试依赖；发布器独立核验生产 EXE、包边界及远端下载字节，资格证明绑定精确提交和 App。
- 修复短脚本退出与最终日志读取的时序问题，以及测试目录准备时进程已退出的清理竞态。
- 两仓库专用 App、受保护 Environment 和 main Ruleset 已启用；Host Release、插件 stable/develop 发布及真实预览下载安装闭环通过。发布资产命名和纯 SHA 文件与更新器契约一致。

## v0.16.6（Pre-release）

### 开发方向

- 围绕影响域门禁、运行时数据安全、通用前端组件与官方插件契约开展当前版本开发。

### 数据安全

- 配置单文件恢复拒绝包含子目录的混合源；复制与移动拒绝目标落入源目录的重叠路径。
- 配置快照部分回滚保留各项备份，文件解锁后可继续恢复并完成幂等收尾。
- JSON 原子写入使用独占临时文件与有界目标锁；损坏配置无法保全时向调用方传播错误。
- JSON 原子写入补充提交前、模拟磁盘不足、提交后、真实文件锁与损坏文件保全失败的阶段边界验证，保留旧值、完整新值和可解释残留。
- 主快照与附加配置事务补充 manifest、commit、metadata 写入失败及重复恢复验证；提交完成后的新快照由下一次恢复收尾。
- 插件 pending journal 使用当前格式严格校验和已验证归属，安装、更新、卸载在隔离 Test Host 经真实 API 与连续重启完成幂等验证。
- 配置门禁使用租约协调等待者、持有者与删除退役。

### 执行与截图

- 游戏进程识别优先按用户填写的游戏路径进程名解析，窗口前置支持延迟启动、失败重试与进程切换。
- 专项脚本实例在运行期间和结束判定阶段动态解析当前游戏进程，并按 Attempt 回退最近有效截图，修复截图失效问题。

### 前端与官方插件

- 公共元素扩展至 38 个，统一动作组、实体行、日期范围、排序与拖动手柄；修复嵌套元素和具名插槽装配。
- 排序由调用方提交数据，支持禁用、重复标识拒绝、取消及焦点恢复；日期范围取消后保留已提交值。
- 官方插件装配测试加载真实公共元素，覆盖交互、卸载和资源清理。

### 测试与文档

- CI 按注册表和调用方依赖选择测试，汇总真实通过、失败与跳过数量；固定跨仓库引用并支持候选 SHA 验证。
- 混合测试容器按现役类型拆分，分域执行使用完整测试集合。
- 架构、插件契约、开发与测试正文迁入专题，原入口保留导航，补充文档索引、迁移账本和解析器链接检查。

## v0.16.5（Pre-release）

### 插件与模拟器

- 新增 Plugin API v1.7 模拟器 provider 扩展契约，雷电、夜神和 BlueStacks 的专属识别与驱动移入可选的 `EmulatorSupport` 插件；Generic ADB 与 MuMuManager 仍由宿主提供。
- GameCheckIn 升级为独立签到任务，支持单个或多个任务、固定时间计划、任务级凭据与通知设置；签到设置移入独立侧栏页面，旧版用户全局签到配置不会自动迁移。
- CustomWallpaper 升级至 0.2.6，壁纸列表改为整卡片与行分隔样式。

### 更新与界面

- “闲时自动更新”在启动服务前检查、下载并应用宿主更新；新增独立的插件自动更新开关，覆盖所有可升级插件。
- 侧栏将“系统”调整为“更多”，统一前端拖拽把手外观，并公开一致的拖拽把手尺寸 token。

## v0.16.4（Pre-release）

### 实时运行观察

- 增加带 Bearer 认证的 `/api/events` SSE 通道，运行状态、日志、系统操作和宿主状态使用统一事件包络与全局序列号发布。
- 前端通过 `fetch` 流式解析事件，断线按退避策略重连；连接不可用、事件丢失或队列溢出时回到 `/api/status` 同步，运行日志最多保留 500 行并显示截断提示。
- 调度中心和仪表盘接入页面范围的实时更新，保留按秒/按 3 秒轮询作为连接降级路径；倒计时继续使用本地 1 秒插值。

### 模拟器驱动

- 增加雷电、夜神和 BlueStacks 实例检测与驱动，分别使用 `ldconsole`、`NoxConsole` 和 `HD-Player`/bundled ADB，Generic ADB 与 MuMuManager 语义保持不变。
- 厂商实例映射采用清单、配置和 ADB 连通性联合确认；无法证明唯一身份时显式报错，关机按厂商优雅命令、ADB 关机和经复核的单 PID 路径收尾。
- 增加隔离 console/ADB/config 夹具与 emulator System Smoke，记录真机验证矩阵到 STATUS。

## v0.16.3（Pre-release）

### 判定脚本探针

- 新增进程、可见顶层窗口和只读 HTTP GET 探针，JavaScript 与 Python 共用统一脚本宿主和回环运行时桥接。
- 探针遵循数量、超时、响应体和输入长度上限；外网请求沿用宿主统一 HTTP 出口，保留截图接口的现有调用契约。
- 补充 Plugin API、插件判断脚本指南以及进程、窗口、HTTP 和 Python 回环调用的契约测试。

### 官方签到插件

- GameCheckIn 升级到 v0.2.0，覆盖米游社、HoYoLAB、森空岛、终末地国际服 / SKPORT 和库街区五个平台的游戏内签到。
- 米游社与 HoYoLAB 增加崩坏 3；森空岛支持明日方舟与终末地，SKPORT 支持终末地，库街区支持鸣潮与战双帕弥什。
- 配置、凭据指纹、运行状态、汇总徽章和中英文商店资料同步扩展到五个平台，沿用当前格式与安全边界。
- GameCheckIn v0.2.1 修复部分社区以非 2xx 状态返回已签到业务结果时的误判，并修复库街区签到月份参数与设备 UUID 格式。

## v0.16.2（Pre-release）

### 文档与维护性

- 文档事实、控制面入口、插件契约与测试门禁按当前实现重新对账。
- 补齐网络通知、Web API 与调度维护性测试，并完成内部职责收敛。
- 插件源码按运行时、数据专用和仓库服务职责归档，补充官方 README 源码目录契约检查。
- 修复官方插件详情 README 地址解析，支持条件缓存、失效缓存回退和无缓存错误提示。

### 仪表盘

- 优化近 7 日历史摘要的状态分布与每日趋势展示。

## v0.16.1（Pre-release）

### 插件会话轮换

- CustomWallpaper 将启动轮换收敛为 Web 运行时会话首次加载时的一次轮换；刷新、轮询、定时轮换和设置操作不会重复推进全局轮换游标。
- CustomWallpaper 升级到 0.2.5，新增 `POST rotation/advance-session` 会话入口，并保留首次会话失败时的状态回退。

### 通用脚本迁移

- 通用脚本支持导出与导入 `.nxpscript.json` 配置文件；文件契约使用版本化 schema，路径按脚本根目录保存为相对或绝对描述符。
- 导入前执行完整校验，保护已有编辑草稿；相对路径在根目录确定后解析，绝对路径会明确提示，名称冲突自动生成唯一名称。

### 控制面与可观察性

- 仪表盘历史摘要增加近 7 日成功/其他趋势、完整状态分布与百分比，保持空数据时的稳定显示。
- 运行计划摘要固定展示任务、用户和完成操作三项指标，任务清单使用列表语义，用户资格表保留状态与今日成功次数。
- 系统诊断按类别分组并显示类别计数；警告与失败提供详情和处理建议，诊断 ID 保留为可访问信息。
- 补充对应的宿主、前端组件、脚本迁移和官方插件回归测试。

## v0.16.0（Pre-release）

### Runtime Efficiency & Durability

- 完成调度状态的 durable replay fence：空闲 600 tick（约 10 分钟）不再产生状态写入，恢复所需 occurrence 与最新调度水位一起保存。
- LogMonitor 改为固定 4 MiB 检查点和固定读取缓冲区，保留追加、截断/重长、替换与深层重写的保守处理边界。
- 同一 Attempt tick 复用进程快照，并按有效消费者决定是否维护最近 PC 截图缓存；1 秒监控节奏和 KN-90 缓存协议保持不变。
- 日志路径解析增加无日期模板与单遍最新文件快路。

### History Insights

- 历史 Web、CLI 和 MCP 查询支持 `success`、`failed`、`partial`、`cancelled`、`skipped` 状态筛选。
- 历史返回投影增加运行/Attempt `durationMs`，新增范围摘要、状态计数、成功率、累计/平均耗时和每日数据；持久化运行 JSON 保持原结构。
- 仪表盘增加近 7 日历史摘要双卡片，历史页聚焦日期、状态、用户和具体运行记录查询。

### Acceptance follow-up

- 更新 worker 等待宿主 EXE 映像句柄释放后再进入程序文件交换，覆盖下次启动更新的 Windows 退出交接窗口。
- 定时扫描与自动更新阻断检查按当前分钟计算，持久化调度水位作为恢复围栏，错过的历史 occurrence 不在启动后补发。
- 更新包进入 `ready` 后隐藏“检查更新”入口，保留立即更新与下次启动更新操作。
- 调度中心运行日志使用每个运行项独立的可访问拖拽/键盘高度调节器，保留滚动和实时画面并列布局。
- 历史摘要从历史页迁移到仪表盘，摘要请求与状态轮询分离并按 60 秒周期刷新。

### Validation / Measurements

- W6 隔离诊断工具对 600 个合成调度 tick 的状态写入计数由 600 降为 0；100 MiB 日志检查点由 104,861,696 字节降为 4,194,304 字节，构造分配由 373,293,816 字节降为 4,260,240 字节，追加读取分配由 373,195,808 字节降为 33,560 字节。
- 同一诊断的构造耗时由 118.50 ms 降为 2.94 ms，追加读取耗时由 241.18 ms 降为 7.19 ms；600 tick 的截图消费者门控计数为无判断脚本 0 次、判断脚本 600 次（1 秒调度、2 秒最大缓存年龄）。前后数据均来自隔离运行时，工具仍为按需诊断资产，不进入默认发布门禁。
- 诊断进程墙钟/CPU 耗时由 471.45/453.13 ms 降为 119.71/93.75 ms，测量结束工作集由 537,374,720 字节降为 39,665,664 字节；该项是合并后的诊断进程指标，用于长期资源趋势对照。


## 历史归档

- [v0.15 系列](docs/history/changelog-v0.15.md)
- [更早版本](docs/history/changelog-earlier.md)
